language: java
jdk:
    - openjdk6
script: "curl http://mirror.nexcess.net/apache/flex/4.9.1/binaries/apache-flex-sdk-4.9.1-bin.tar.gz && tar -zxvf apache-flex-sdk-4.9.1-bin.tar.gz && ls && ./flash-cli -d"
notifications:
  recipients:
    - dias.h.bruno@gmail.com
  email:
    on_success: change
    on_failure: always
